{
  "name": "studio-pro-automation",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "CLI automation for Studio Pro — render videos from markdown/JSON scripts using system Chrome",
  "scripts": {
    "render": "node render.js",
    "batch": "node batch.js"
  },
  "dependencies": {
    "puppeteer-core": "^23.0.0"
  }
}
